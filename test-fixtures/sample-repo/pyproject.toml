[project]
name = "debt-demo-api"
version = "0.1.0"
description = "A small FastAPI service with deliberate architectural debt, used as the target repo for the refactoring harness."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
